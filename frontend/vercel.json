{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --frozen-lockfile || npm install && npm build",
  "installCommand": "npm install",
  "outputDirectory": "frontend/.next",
  "functions": {
    "api/**": { "runtime": "nodejs18.x" }
  },
  "env": {
    "NEXT_RUNTIME": "nodejs"
  }
}
