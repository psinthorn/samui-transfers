# 🎨 Email Verification System - Visual Architecture

## System Architecture Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                      SAMUI TRANSFERS                           │
│                Email Verification System                       │
└────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   USER ACTIONS      │
├─────────────────────┤
│ • Sign Up           │
│ • Verify Email      │
│ • Login             │
│ • Reset Password    │
└──────────┬──────────┘
           │
           ▼
╔═════════════════════════════════════════════════════════════════╗
║                    AUTHENTICATION LAYER                        ║
║                      (auth.ts)                                 ║
╠═════════════════════════════════════════════════════════════════╣
║                                                                 ║
║  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐  ║
║  │ Registration │  │    Login     │  │  Password Reset     │  ║
║  │ Process      │  │  Process     │  │  Process            │  ║
║  │              │  │              │  │                     │  ║
║  │ • Validate   │  │ • Check user │  │ • Validate email    │  ║
║  │ • Hash pwd   │  │ • Verify pwd │  │ • Generate token    │  ║
║  │ • Create usr │  │ • CHECK      │  │ • Send email        │  ║
║  │ • Gen token  │  │   EMAIL ✓✓✓  │  │ • Validate token    │  ║
║  │ • Send mail  │  │ • Issue JWT  │  │ • Update password   │  ║
║  └──────────────┘  └──────────────┘  └─────────────────────┘  ║
║                                                                 ║
╚═════════────────────┬──────────────────────────────────────────╝
                      │
           ┌──────────┴──────────┐
           │                     │
           ▼                     ▼
    ┌──────────────┐      ┌────────────────┐
    │ Email Module │      │ Database       │
    │ (email.ts)   │      │ (Prisma)       │
    └──────────────┘      └────────────────┘
           │                     │
    ┌──────┴──────┐        ┌─────┴──────┐
    │             │        │            │
    ▼             ▼        ▼            ▼
┌─────────┐  ┌─────────┐ ┌──────┐  ┌──────────┐
│ Verify  │  │ Password│ │Users │  │Verification
│ Email   │  │ Reset   │ │Table │  │Tokens Table
└─────────┘  └─────────┘ └──────┘  └──────────┘

Parameters: Sends verification
- email      tokens that expire
- name       in 24 hours
- token
```

---

## Data Flow Diagram

### Registration Flow
```
┌─────────────────────────────────────────┐
│ 1. User Submits Registration Form       │
│    (email, password, name)              │
└────────────────┬────────────────────────┘
                 │
                 ▼
        ┌─────────────────┐
        │ Validate Input  │
        │ • Check email   │
        │ • Check pwd str │
        └────────┬────────┘
                 │
                 ▼
        ┌─────────────────┐
        │ Check Duplicate │
        │ Email exists?   │
        └────────┬────────┘
                 │ No
                 ▼
        ┌─────────────────┐
        │ Hash Password   │
        │ bcryptjs (12r)  │
        └────────┬────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ Create User in Database     │
        │ emailVerified: null         │
        │ password: hashed            │
        └────────┬────────────────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ Generate Token              │
        │ • 32 bytes random           │
        │ • Expires: +24 hours        │
        └────────┬────────────────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ Store Token in DB           │
        │ VerificationToken table     │
        │ identifier: email           │
        │ token: token                │
        │ expires: date               │
        └────────┬────────────────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ Send Verification Email     │
        │ HTML template with link:    │
        │ /verify-email?              │
        │   token=XXX                 │
        │   email=user@example.com    │
        └────────┬────────────────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ User Receives Email ✉️      │
        └─────────────────────────────┘
```

### Verification Flow
```
┌──────────────────────────────────┐
│ User Clicks Verification Link    │
│ /verify-email?token=ABC...       │
└────────────────┬─────────────────┘
                 │
                 ▼
        ┌─────────────────────┐
        │ Extract Parameters  │
        │ • token             │
        │ • email             │
        └────────┬────────────┘
                 │
                 ▼
        ┌─────────────────────┐
        │ API POST /verify    │
        └────────┬────────────┘
                 │
                 ▼
        ┌─────────────────────────────┐
        │ Query VerificationToken DB  │
        │ WHERE identifier = email    │
        │   AND token = token         │
        └────────┬────────────────────┘
                 │
         ┌───────┴────────┐
         │ No             │ Yes
         ▼                ▼
    ERROR ❌      Check Expiry
    "Invalid       ┌─────────────┐
     token"        │ Expired?    │
                   └──┬────────┬─┘
                  Yes│        │ No
                      ▼       ▼
                  ERROR ❌   UPDATE USER
                  "Expired"  emailVerified
                            = NOW()
                   ┌──────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Delete Token from DB │
        │ (one-time use)       │
        └────────┬─────────────┘
                 │
                 ▼
        ┌──────────────────────┐
        │ Return Success       │
        │ "Email verified!"    │
        └────────┬─────────────┘
                 │
                 ▼
        ┌──────────────────────┐
        │ User Can Now Login   │
        │ ✅ emailVerified ✓   │
        └──────────────────────┘
```

### Login Flow
```
┌─────────────────────────────────┐
│ User Submits Login Form         │
│ (email, password)               │
└────────────────┬────────────────┘
                 │
                 ▼
        ┌─────────────────────┐
        │ Find User by Email  │
        │ Query DB            │
        └────────┬────────────┘
                 │
         ┌───────┴────────┐
         │ No             │ Yes
         ▼                ▼
    ERROR ❌      ┌──────────────┐
    "Not found"   │ Compare Hash │
                  │ bcryptjs     │
                  └──┬─────────┬─┘
                  No │         │Yes
                     ▼         ▼
                 ERROR ❌  ┌────────────────┐
                 "Wrong    │ Check: Disabled
                  pwd"     │ if (disabled)
                          └──┬──────────┬──┘
                          Yes│          │No
                             ▼          ▼
                         ERROR ❌  ┌───────────────┐
                         "Disabled"│Check: Email
                                   │Verified
                                   │if (!emailVer)
                                   └──┬──────┬────┘
                                   No │      │Yes
                                      ▼      ▼
                                  ERROR ❌  ✅ SUCCESS
                                  "Verify    • Create
                                   email"     JWT token
                                            • Start
                                              session
                                            • Redirect
                                              to /dash
```

---

## Database Schema Visualization

```
┌──────────────────────────────────────────┐
│            DATABASE TABLES               │
└──────────────────────────────────────────┘

USER Table:
┌──────────────────────────────┐
│ id (UUID)                    │ ← Primary Key
│ email (String)               │ ← Unique
│ emailVerified (DateTime?)    │ ← NULL = unverified
│ password (String?)           │ ← bcryptjs hash
│ name (String?)               │
│ role (String)                │ ← "USER" or "ADMIN"
│ disabled (Boolean)           │ ← Account suspension
│ preferredLanguage (String)   │
│ marketingEmails (Boolean)    │
│ createdAt (DateTime)         │
│ updatedAt (DateTime)         │
└──────────────────────────────┘
         │
    ONE-TO-MANY
         │
         ▼
VerificationToken Table:
┌──────────────────────────────┐
│ identifier (String)          │ ← User email
│ token (String)               │ ← 32-byte random hex
│ expires (DateTime)           │ ← 24 hours from creation
│ @@unique(identifier, token)  │
└──────────────────────────────┘

User Relationships:
  User ──1:N──→ Account (OAuth)
  User ──1:N──→ Session (JWT)
  User ──1:N──→ Booking (transfers)
  User ──1:N──→ AuditLog (activity)
```

---

## Security Layers Visualization

```
┌────────────────────────────────────────────┐
│     SECURITY LAYERS (Defense in Depth)     │
└────────────────────────────────────────────┘

Layer 1: AUTHENTICATION
┌──────────────────────────────────────────┐
│ • Email verification required ✓✓         │
│ • Password hashing (bcryptjs) ✓✓         │
│ • Account disabled check ✓               │
│ • JWT token management ✓                 │
└──────────────────────────────────────────┘
         │
         ▼
Layer 2: EMAIL VERIFICATION
┌──────────────────────────────────────────┐
│ • 32-byte random tokens ✓✓               │
│ • One-time use (deleted after) ✓         │
│ • 24-hour expiration ✓                   │
│ • Database storage (not cookie) ✓✓       │
└──────────────────────────────────────────┘
         │
         ▼
Layer 3: RATE LIMITING
┌──────────────────────────────────────────┐
│ • Verify: 5 per 15 min ✓                 │
│ • Resend: 3 per 10 min ✓                 │
│ • Reset: 3 per 10 min ✓                  │
│ • HTTP 429 on limit ✓                    │
└──────────────────────────────────────────┘
         │
         ▼
Layer 4: SMTP SECURITY
┌──────────────────────────────────────────┐
│ • SMTP authentication required ✓✓        │
│ • TLS encryption support ✓               │
│ • Valid sender email ✓✓                  │
│ • HTML templates (no injection) ✓        │
└──────────────────────────────────────────┘
         │
         ▼
Layer 5: TOKEN SECURITY
┌──────────────────────────────────────────┐
│ • Crypto.randomBytes() ✓✓                │
│ • No token in logs ✓                     │
│ • Token cleanup after use ✓              │
│ • Unique per email ✓                     │
└──────────────────────────────────────────┘
```

---

## Configuration Paths

```
┌─────────────────────────────────────────┐
│    CONFIGURATION HIERARCHY              │
└─────────────────────────────────────────┘

.env.local (Highest Priority)
  ├─ EMAIL_FROM
  ├─ MAILTRAP_HOST (optional)
  ├─ SMTP_HOST (optional)
  └─ DATABASE_URL
       │
       ▼
.env (Default Values)
  ├─ EMAIL_FROM fallback
  └─ Default settings
       │
       ▼
Code Defaults
  ├─ Nodemailer config
  ├─ SMTP fallback
  └─ Error messages

Precedence: .env.local > .env > Code Defaults
```

---

## API Endpoints Map

```
┌─────────────────────────────────────────┐
│      EMAIL VERIFICATION API             │
└─────────────────────────────────────────┘

Authentication Routes:
/api/auth
├─ POST /callback/credentials  → Login
├─ GET /session                → Session info
├─ GET /csrf                   → CSRF token
├─ POST /signin                → Sign in form
├─ POST /signout               → Sign out
└─ POST /verify-email
   ├─ POST (verify)
   │   ├─ Input: {email, token}
   │   └─ Output: {success, message}
   │
   └─ PUT (resend)
       ├─ Input: {email}
       └─ Output: {success, message}

Password Routes:
/api/auth/password-reset
├─ POST (request reset)
│   ├─ Input: {email}
│   └─ Output: {success, message}
│
└─ PUT (confirm reset)
    ├─ Input: {email, token, password}
    └─ Output: {success, message}
```

---

## Test Flow Diagram

```
┌─────────────────────────────────────────┐
│         TESTING SCENARIOS               │
└─────────────────────────────────────────┘

┌─ Pre-Verified Users ─────────────────────┐
│ user@test.com / Test_123!  ✅ WORKS     │
│ admin@admin.com / Admin_123! ✅ WORKS   │
│ john@example.com / John_123! ✅ WORKS   │
└─────────────────────────────────────────┘

┌─ New User Registration ──────────────────┐
│ 1. Sign up ──────────────────────── OK   │
│ 2. Receive email ──────────────── OK     │
│ 3. Click link ─────────────────── OK     │
│ 4. Email verified ────────────── OK      │
│ 5. Login ──────────────────────── OK     │
│ Result: ✅ WORKS                         │
└─────────────────────────────────────────┘

┌─ Password Reset Flow ─────────────────────┐
│ 1. Request reset ──────────────── OK      │
│ 2. Receive email ──────────────── OK      │
│ 3. Click link ─────────────────── OK      │
│ 4. New password ───────────────── OK      │
│ 5. Login with new pwd ─────────── OK      │
│ Result: ✅ WORKS                          │
└──────────────────────────────────────────┘

┌─ Unverified User Block ───────────────────┐
│ User emailVerified: null                 │
│ Try to login ──────────────── BLOCKED ❌  │
│ Error message: "Please verify email"    │
│ Result: ✅ CORRECT BEHAVIOR              │
└──────────────────────────────────────────┘
```

---

## Quick Reference Card

```
┌─────────────────────────────────────────┐
│        QUICK REFERENCE CARD             │
└─────────────────────────────────────────┘

✅ VERIFICATION REQUIRED: YES
✅ EMAIL CHECK ON LOGIN: ENABLED
✅ TOKEN EXPIRY: 24 hours
✅ TOKEN LENGTH: 32 bytes
✅ PASSWORD HASHING: bcryptjs (12)
✅ RATE LIMITING: Enabled (5/15min)
✅ TEST USERS: 4 pre-verified
✅ DOCUMENTATION: Comprehensive
✅ STATUS: PRODUCTION READY

Test Login:
  user@test.com / Test_123!

New Registration:
  /sign-up → Verify email → Login

Files Modified:
  1. .env.local
  2. prisma/seed.ts
  3. auth.ts
  4. Documentation (5 files)
```

